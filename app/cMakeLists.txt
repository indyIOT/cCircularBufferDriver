add_executable(${EXECUTABLE_NAME}  source/main.c)

target_include_directories(${EXECUTABLE_NAME} PRIVATE include)
target_include_directories(${EXECUTABLE_NAME}  PRIVATE ../../cCommonMacros/include )
target_include_directories(${EXECUTABLE_NAME}  PRIVATE ../../cCommonTypes/include )
target_include_directories(${EXECUTABLE_NAME} PRIVATE ../cCircularBufferDriverLib/publicInclude)
if ( USE_MUTEXES )
    target_include_directories(${EXECUTABLE_NAME} PRIVATE ../../cMutexDriver/cMutexDriverLib/publicInclude)
endif()

target_link_libraries(${EXECUTABLE_NAME} PUBLIC ${CIRCULAR_BUFFER_DRIVER_LIBRARY_NAME} )
